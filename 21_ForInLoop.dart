void main(List<String> args) {
  // for-in loop
  var numbers = [1, 2, 3];

  for (var i in numbers) {
    print(i);
  }

  // Using standard for loop
  for (var i = 0; i < numbers.length; i++) {
    print(numbers[i]);
  }
}